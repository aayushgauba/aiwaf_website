# Environment Configuration for AIWAF Documentation Website

# =======================================================
# DEVELOPMENT MODE (Local with SSH Tunnel)
# =======================================================
# Set FLASK_ENV=development to enable SSH tunnel mode
FLASK_ENV=development
FLASK_DEBUG=True

# SSH Tunnel Configuration (for PythonAnywhere)
SSH_HOST=ssh.pythonanywhere.com
SSH_USERNAME=your_pythonanywhere_username
SSH_PASSWORD=your_ssh_password

# Database Configuration (PythonAnywhere MySQL)
DB_USER=your_pythonanywhere_username
DB_PASSWORD=your_mysql_password
DB_HOST=your_username.mysql.pythonanywhere-services.com
DB_PORT=3306
DB_NAME=your_username$your_database_name

# =======================================================
# PRODUCTION MODE (Direct Connection)
# =======================================================
# Set FLASK_ENV=production to disable SSH tunnel
# FLASK_ENV=production
# FLASK_DEBUG=False

# Option 1: Use DATABASE_URL (recommended for platforms like DigitalOcean)
# DATABASE_URL=mysql+pymysql://username:password@host:port/database

# Option 2: Use individual components (same as above)
# DB_USER=your_username
# DB_PASSWORD=your_password
# DB_HOST=your_database_host
# DB_PORT=3306
# DB_NAME=your_database_name

# =======================================================
# COMMON SETTINGS
# =======================================================
SECRET_KEY=your-secret-key-change-in-production
PORT=5000

# =======================================================
# DEPLOYMENT INSTRUCTIONS
# =======================================================
# 
# For Local Development:
# 1. Copy this file to .env
# 2. Fill in your PythonAnywhere SSH and MySQL credentials
# 3. Set FLASK_ENV=development
# 4. Run: python run_tunnel.py
#
# For DigitalOcean Deployment:
# 1. Set environment variables in App Platform:
#    - FLASK_ENV=production
#    - DATABASE_URL=mysql+pymysql://user:pass@host:port/db
#    - SECRET_KEY=your-production-secret
# 2. Use production.py as startup command
#
# For Other Production Deployments:
# 1. Set FLASK_ENV=production
# 2. Set database credentials (DB_USER, DB_PASSWORD, etc.)
# 3. Run: python app.py
#